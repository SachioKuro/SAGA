{
    "tasks": [
        {
            "type": "shell",
            "label": "build",
            "command": "${workspaceFolder}/build-macos.sh",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build",
            "detail": "Main compile Task."
        }
    ],
    "version": "2.0.0"
}